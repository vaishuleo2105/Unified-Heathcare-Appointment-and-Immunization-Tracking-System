# TODO

- [x] Update `server/routes/auth.js` to return detailed error messages to frontend when `NODE_ENV !== 'production'`.
- [x] Ensure 500 responses include `err.message` and keep generic message for production.

- [x] Restart server and retry login/register to verify the real backend error is visible in UI.

